<article class="text-left border-b-2 border-stone-900 py-10">
  <div class="mb-6 flex flex-col gap-4 text-lg"><%= simple_format(scribble.content) %></div>
  <p class="text-xs">On <%= scribble.updated_at.in_time_zone(current_user.timezone).strftime("%B %d, %Y at %I:%M %p") %></p>
</article>
